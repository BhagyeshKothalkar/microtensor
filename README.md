to build, run from the project root:

```bash
./scripts/build.sh
```
# microtensor


current failures: 
The following tests FAILED:
         16 - TensorTests.TestBroadcastToShape (Failed) (fails randomly (on random inputs))
         22 - TensorTests.TestLinearLayer (Subprocess aborted)
Errors while running